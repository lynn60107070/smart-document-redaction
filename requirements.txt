# Use a project virtual environment to avoid dependency conflicts.
# spaCy 3.7+ is required for Pydantic v2 compatibility.
# PyTorch is not a project dependency; if you see a pynvml/nvidia warning,
# use a clean venv or:
# pip install nvidia-ml-py && pip uninstall pynvml (only if you use PyTorch).

# Core app
streamlit
fastapi
uvicorn
python-multipart

# NLP (compatible with Pydantic v2)
spacy>=3.7.0,<4

# PDF processing
pymupdf
pdfplumber

# Data processing
pandas
numpy

# ML / utilities
tqdm
scikit-learn
matplotlib

# Testing
pytest
httpx
