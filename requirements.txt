# Use a project venv to avoid spaCy/Pydantic conflicts with other global packages.
# PyTorch is not a project dependency; if you see a pynvml/nvidia warning, use a
# clean venv, or: pip install nvidia-ml-py && pip uninstall pynvml (only if you use PyTorch).

streamlit
# spaCy 3.7+ is required for Pydantic v2 compatibility (model loading from disk).
spacy>=3.7.0,<4
pymupdf
pdfplumber
pandas
numpy
tqdm
scikit-learn
matplotlib