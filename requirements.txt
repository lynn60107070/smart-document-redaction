# Use a dedicated virtual environment (avoids PyTorch / NVIDIA / PyMuPDF clashes on some setups).
# spaCy 3.7+ matches Pydantic v2; keep pydantic v2 for load_ner_model / FastAPI (see ai_model/ner_model.py).
#
# Training baseline weights (NOT on PyPI): after `pip install -r requirements.txt`, run:
#   python -m spacy download en_core_web_lg
# Required for ai_model/train_model.py default --pretrained en_core_web_lg (~800 MB).

# --- API & server ---
fastapi>=0.109.0,<1
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic>=2.4.0,<3

# --- Optional UI (.streamlit/ in .gitignore; no Python entrypoint in repo yet) ---
streamlit>=1.28.0

# --- NLP ---
spacy>=3.7.0,<4

# --- PDF (code uses PyMuPDF / fitz only) ---
pymupdf>=1.23.0

# --- Data / preprocessing / evaluation plots ---
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.66.0
scikit-learn>=1.3.0
matplotlib>=3.7.0

# --- Notebooks (notebooks/*.ipynb: EDA uses seaborn; train split uses sklearn) ---
jupyter>=1.0.0
ipykernel>=6.0.0
seaborn>=0.12.0

# --- Testing (FastAPI TestClient uses httpx) ---
pytest>=7.4.0
httpx>=0.26.0
